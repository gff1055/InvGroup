/*const db = require('./db')

const NomeTabela = db.sequelize.define('dados', {
    atributo: {
        tipo: db.Sequelize.STRING
    },
    atributo: {
        tipo: db.Sequelize.TEXT
    }
});

NomeTabela.sync({force: if true})

module.exports = NomeTabela*/